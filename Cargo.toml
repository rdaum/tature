[package]
name = "tature"
version = "0.1.0"
edition = "2021"
authors = ["Ryan Daum"]
description = "Port of classic regexpr.c regex engine to Rust with tick-based execution limits"
license = "MIT"

[dependencies]

[dev-dependencies]